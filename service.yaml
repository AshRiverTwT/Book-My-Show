apiVersion: v1
kind: Service
metadata:
  namespace: bms-ns
  name: bookmyshow-service
  labels:
    app: bookmyshow
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: bookmyshow

